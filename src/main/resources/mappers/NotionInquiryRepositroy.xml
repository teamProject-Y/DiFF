<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.repository.NotionInquiryRepository">

    <insert id="saveInquiry" parameterType="com.example.demo.vo.NotionInquiry" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO notionInquiry (title, nickName, email, body, regDate)
        VALUES (#{title}, #{nickName}, #{email}, #{body}, NOW())
    </insert>

    <update id="updatePageId">
        UPDATE notionInquiry
        SET notionPageId = #{notionPageId}
        WHERE id = #{id}
    </update>

</mapper>
