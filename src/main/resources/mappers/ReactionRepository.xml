<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.repository.ReactionRepository">

    <insert id="like" parameterType="map">
        INSERT INTO `reaction` set
        memberId = #{memberId},
        relType = #{relType},
        relId = #{relId}
    </insert>

    <delete id="unlike" parameterType="map">
        DELETE FROM reaction
        WHERE relType = #{relType}
        AND relId = #{relId}
        AND memberId = #{memberId}
    </delete>

    <select id="getCount" resultType="long">
        SELECT COUNT(*)
        FROM reaction
        WHERE relType = #{relType} AND relId = #{relId}
    </select>

    <select id="isLiked" parameterType="map">
        SELECT COUNT(*)
        FROM reaction
        WHERE relType = #{relType}
        AND relId = #{relId}
        AND memberId = #{memberId}
    </select>

</mapper>