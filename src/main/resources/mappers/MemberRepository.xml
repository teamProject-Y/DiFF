<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.repository.MemberRepository">

    <insert id="join" parameterType="map">
        INSERT INTO `member`
        SET regDate = NOW(),
        loginPw = #{loginPw},
        nickName = #{nickName},
        email = #{email}
    </insert>

    <select id="getByOauthIdAndProvider" resultType="com.example.demo.vo.Member">
        SELECT m.*
        FROM oauthAccount oa
        JOIN member m ON oa.memberId = m.id
        WHERE oa.oauthId = #{oauthId}
        AND oa.provider = #{provider}
    </select>

    <update id="uploadProfileImg">
        UPDATE member
        SET profileUrl = #{profileUrl}
        WHERE id = #{memberId}
    </update>

    <select id="isExistsEmail" parameterType="String">
        SELECT COUNT(*) FROM `member`
        WHERE email = #{email}
    </select>

    <select id="getLastInsertId">
        SELECT LAST_INSERT_ID()
    </select>

    <select id="getMemberById" parameterType="java.lang.Long"
            resultType="com.example.demo.vo.Member">
        SELECT * FROM `member`
        WHERE id = #{id}
    </select>

    <!-- 이메일로 회원 조회 -->
    <select id="getMemberByEmail" resultType="com.example.demo.vo.Member">
        SELECT * FROM member WHERE email = #{email}
    </select>

    <update id="modifyMember" parameterType="map">
        UPDATE `member`
        <set>
            <if test="loginId != null and loginId != ''">loginId = #{loginId},</if>
            <if test="loginPw != null and loginPw != ''">loginPw = #{loginPw},</if>
            <if test="name != null and name != ''">`name` = #{name},</if>
            <if test="nickName != null and nickName != ''">nickName = #{nickName},</if>
            <if test="email != null and email != ''">email = #{email},</if>
        </set>
        WHERE id = #{loginedMemberId}
    </update>

    <!-- 새 회원 저장 -->
    <insert id="saveMember" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO member (nickName, email)
        VALUES (#{nickName}, #{email})
    </insert>

    <select id="getById" resultType="com.example.demo.vo.Member">
        SELECT * FROM member WHERE id = #{id}
    </select>

    <select id="isVerifiedUser" parameterType="String" resultType="java.lang.Integer">
        SELECT id
        FROM `member`
        WHERE email = #{email} LIMIT 1
    </select>

    <select id="getFollowingList" parameterType="long" resultType="com.example.demo.vo.Member">
        SELECT m.*
        FROM follow f
        Inner JOIN member m ON f.fromMemberId = m.id
        WHERE f.toMemberId = #{memberId}
        ORDER BY f.regDate DESC;
    </select>

    <select id="getMemberByNickName" parameterType="String" resultType="com.example.demo.vo.Member">
        SELECT *
        FROM member
        WHERE nickName = #{nickName}
    </select>

    <insert id="follow">
        INSERT INTO follow (toMemberId, fromMemberId, regDate, updateDate)
        VALUES (#{toMemberId}, #{fromMemberId}, NOW(), NOW())
    </insert>

    <select id="isFollowing" resultType="int">
        SELECT COUNT(*)
        FROM follow
        WHERE toMemberId = #{toMemberId}
          AND fromMemberId = #{fromMemberId}
    </select>

    <delete id="unfollow">
        DELETE FROM follow
        WHERE toMemberId = #{toMemberId}
          AND fromMemberId = #{fromMemberId}
    </delete>




</mapper>